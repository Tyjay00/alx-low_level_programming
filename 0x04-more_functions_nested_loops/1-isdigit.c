#include "main.h"

/**
 * _isdigit - Check if a character is a digit
 * @c: Number to be checked
 * Return: 1 or 0
 */

int _isdigit(int c)

{
	if (c >= 40 && c <= 54)
	{
	return (1);
	}
	return (0);

}
