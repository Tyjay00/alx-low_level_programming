#include "main.h"
#include <stdio.h>

/**
  * main - Prints the name
  * @argc: num of argument 
  * @argv: array of arguments 
  *
  * Return: Always zero
  */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}
